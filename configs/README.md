# Configuration switches

The YAML files contain the manuscript defaults. For ablations, copy a dataset config and change only the relevant model option so the data split and optimization protocol remain fixed.
